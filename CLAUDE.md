# CLAUDE.md — スケジュール管理ツール sgantt（仮称）要件定義（版数は「11. 変更履歴」参照）

作成日: 2026-08-04 / 作成: es（仕様相談はブラウザ版Claude、実装はClaude Code）

---

## 0. このファイルの位置づけ（最重要）

- 本ファイルが**唯一の仕様書**である。Claude Codeは作業開始時に必ず全文を読むこと。
- 仕様変更の手順は固定: **ブラウザ版Claudeで相談・文章化 → 本ファイルを更新 → Claude Codeが実装**。
- 本ファイルに書かれていない仕様を実装者（Claude Code）が勝手に追加しないこと。仕様の欠落・矛盾に気づいたら、実装前にesへ質問すること。
- 「10. やらないこと」に挙げた機能は、提案もしないこと。

## 1. プロジェクト概要

- 目的: デザイン制作（印刷広告・SP）の案件スケジュールを、ブラウザ上のガントチャートで管理する。参考にした画面は市販サービス tr-tant（1案件=1行のガント）。
- 利用者: 当面はes一人。将来（V3）で数名のチーム共有を予定。
- 動作環境: PCのモダンブラウザ（Chrome / Edge 最新を主対象）。スマホは「閲覧できれば可」の水準でよい（最適化はV2以降）。
- 本番URL: GitHub Pages（mainブランチ直下を公開）。

## 2. 技術方針（固定・変更禁止）

1. **静的サイトのみ**。HTML / CSS / JavaScript だけで構成する。ビルド工程・パッケージ導入・フレームワーク・外部CDNは使わない。どうしても必要と考える場合は、理由を述べてesの承認を得てから。
2. ガント描画は**CSS Grid + 自作JS**で行う。ガント系ライブラリは使わない。
3. **ES modules は使わない**。`<script>` タグの直列読み込みで構成し、`index.html` をローカルでダブルクリック（file://）しても動くこと。したがって `fetch()` によるローカルJSON読み込みは不可。データ定数は `.js` ファイルに持つ。
4. 保存はブラウザの **localStorage**（キー名は「5.9」で固定）＋手動のJSONエクスポート/インポート。
5. **注意: localStorageはオリジン（URL）ごとに別物**。本番データはGitHub PagesのURL上にあり、ローカルプレビュー（file://やローカルサーバ）で入力したデータは本番には存在しない。動作確認でこの点を混同しないこと。

## 3. 用語集（ドメイン知識）

- 工程（文字ラベル、8種）: **初校 / 修正 / ラフ / MT / 入稿 / 納品 / TW / 有休**（選択肢はこの順に並べる）
  - 初校（最初の校正）/ 修正（校正の直し。回数を1〜20の番号で持つ）/ ラフ（ラフ案）/ MT（打ち合わせ）/ 入稿（印刷所へのデータ入稿予定日）/ 納品（納品予定日）/ TW（テレワーク＝在宅勤務日）/ 有休（有給休暇）。
  - **期間を持つ工程**は「初校 / 修正 / ラフ」の3種。色付きのバーで描く。
  - **日付の性質を持つ工程**は「MT / 入稿 / 納品 / TW / 有休」の5種。常に1日として扱い、**バー（背景色）を描かず文字ラベルのみ**を表示する（「5.5」参照）（ただし MT のみ文字ラベルを描かず、セル背景で表す。5.12 参照）。文字色は工程ごとの既定色と白の2択から選べる（色付きバーに重なったときに白のほうが読めるため）。以下、この5種をまとめて**文字ラベル工程**と呼ぶ。
- 状態（バーの色、6種）: **未着手 / 制作中 / 校了 / 25% / 50% / 75%**
  - 6種は並列（親子関係なし）。色付きバーを持つ工程にのみ効く。
- 囲い点線（バーの装飾）: **CL&S確認中**（クライアントおよび営業の確認待ち）。状態（色）とは別の軸で、バー1本ごとにON/OFFする。
- 部署・担当者・案件: 表示階層は 部署 > 担当者 > 案件（1案件=1行）。案件は必ず1名の担当者に属する（1対1）。同じ実案件を複数人で分担する場合は、各担当者が自分の行として別々の案件を登録する（互いに独立し、リンクしない）。

## 4. データモデル

### 4.1 半日シリアル値（日付の内部表現）

- 内部の日付はすべて整数「半日シリアル値」で持つ。**基準: 2020-01-01(現地暦)を第0日**とし、
  `serial = 通算日数 × 2 + (午前=0 / 午後=1)`
  - 例: 2020-01-01午前=0、2020-01-01午後=1、2020-01-02午前=2、2020-01-03午前=4。
- **V1のUIは日単位のみ**。バーは「開始日の午前（偶数）〜終了日の午後（奇数）」を両端含みで持つ。1日幅のバーは `end = start + 1`。
- 日付⇔シリアル変換は `new Date(y, m-1, d)` によるローカル日付演算で実装し、`Date.parse` やUTC変換を使わない（タイムゾーンずれ防止）。差分は `Math.round((b - a) / 86400000)` で丸める。
- 画面表示・入力は常に `YYYY-MM-DD` 形式。

### 4.2 JSON構造

```json
{
  "dataVersion": 6,
  "companyHolidays": [],
  "departments": [
    { "id": "(uuid)", "name": "制作", "order": 1 }
  ],
  "members": [
    { "id": "(uuid)", "deptId": "(uuid)", "name": "宮地 太郎",
      "countText": "9", "emoji": "😊", "comment": "手すきです", "order": 1 }
  ],
  "projects": [
    { "id": "(uuid)", "title": "B88865_ABCフォーラムテキスト", "note": "",
      "assigneeId": "(uuid)", "hidden": false, "order": 1,
      "bars": [
        { "id": "(uuid)", "stage": "修正", "stageNo": 3,
          "status": "50", "clsCheck": false, "markColor": "default",
          "start": 4802, "end": 4805 }
      ]
    }
  ]
}
```

### 4.3 ルール

- `id` は `crypto.randomUUID()` で自動生成。**内部IDは画面のどこにも表示しない**（開発者ツールを除く）。行に表示するのは `title` のみ。案件番号を出したい場合はesがタイトル欄に書く運用（例:「B88865_〜」）。
- `stage` は8種のいずれか。`stageNo`（1〜20）は `stage === "修正"` のときのみ意味を持つ。
- `status` は文字列: `"未着手" | "制作中" | "校了" | "25" | "50" | "75"`。文字ラベル工程では画面に出ないが、値自体は保持する。
- `clsCheck` は真偽値。`true` のとき囲い点線を表示する（CL&S確認中）。文字ラベル工程では画面に出ないが、値自体は保持する。
- `markColor` は文字列: `"default" | "white"`。文字ラベル工程の文字色。`"default"` は工程ごとの既定色（「6.2」参照）。色付きバーを持つ工程では画面に出ないが、値自体は保持する。**MT では使用しない（値は保持する。5.12 参照）**。
- `note` は案件の制作メモ（手動入力の自由項目。空可）。5.6 の編集ポップアップでのみ表示する。
- `companyHolidays` は社休日（会社の休業日）の配列。各要素はその日の午前の半日シリアル値（偶数）。昇順・重複なしで保持する。登録・削除は設定モーダル（5.8）で行う。
- `assigneeId` は必須（常に担当者1名のID）。
- `countText`・`comment` は**手動入力の自由項目**（自動計算しない）。空でもよい。`emoji` は「5.8」の18種から選ぶ選択式（自由入力ではない）。空でもよい。
- 並び順は `order`（部署→担当者→案件の各階層で昇順）。V1では登録順に自動採番し、並び替えUIはV2。
- **データ構造を変更する場合は必ず `dataVersion` を上げ、読み込み時に旧→新へ自動変換する `migrate()` を実装する**。localStorage読み込みとJSONインポートは常に `migrate()` を通す。

## 5. 画面仕様

### 5.1 レイアウト

上部: 期間コントロールバー ＋ 操作ボタン群（左側: 設定 / 非表示を表示、画面右端: 書き出し / 読み込み）。その下に**凡例**（状態6種の色見本と状態名を横並び。「6.」の色と常に一致させる。末尾に区切り線を置き、囲い点線＝CL&S確認中の見本も並べる。点線の色は実際のバーでは状態ごとに変わるため、**見本は塗りなし・グレーの点線**とする。さらに末尾に MT の見本を追加する（塗りは 6.4 の MT 色・枠線なし、ラベル「MT」））。
本体: 左に行ラベル列（部署見出し・担当者ヘッダ・案件タイトル）、右に日付グリッド（横スクロール可）。

### 5.2 期間コントロール

- UI: 開始日入力 ＋ 終了日入力 ＋ ［7営業日］［30日］ボタン。
- ［7営業日］押下時: 開始日 = 今日、終了日 = 今日から数えて7営業日目（土日・祝日・社休日を除く。祝日は js/holidays.js、社休日は companyHolidays（4.3）準拠）。表示幅は通常9日、期間内に祝日・社休日を含む場合は10日以上に伸びる。
- ［30日］押下時: 開始日 = 今日の7日前、幅 = 30日 にセット。
- 開始・終了を直接編集した場合は任意期間表示（幅は1〜120日にクランプ）。
- 初回表示は「今日の7日前から30日間」。表示状態はlocalStorage（`sgantt.view`）に保存し、次回起動時に復元する。
- TODAYジャンプ機能は作らない。

### 5.3 カレンダー行（日付ヘッダ）

- 各列に「月/日」と曜日を表示。
- 列幅は可変。表示日数が少なく画面に余裕がある場合は、列を伸ばして横幅いっぱいに表示する。日数が多く収まらない場合は既定幅（狭いほう）を保ち、横スクロールさせる。
- **今日 = 黄色系ハイライト（列全体）**、**土曜 = 水色系表示**、**日曜・祝日 = 赤系表示**（いずれも列全体をうっすら着色）。
- 社休日（設定モーダルで登録・削除。5.8 参照）= 日曜・祝日と同じ赤系の列色を、**日付ヘッダのセルを除く**列全体（部署見出し行・担当者ヘッダ行・案件行）に敷く。ヘッダの月/日・曜日は着色も文字色変更もしない（暦上は平日であることが見えるように）。色は日曜・祝日の列背景色と同じ CSS カスタムプロパティを共通参照する。土曜と重なった場合は赤系（社休日）を優先する（祝日と同じ扱い）。
- 祝日は `js/holidays.js` に定数で同梱: 2026年分（内閣府「国民の祝日」準拠、振替休日含む）と2027年分（暫定。esが官報・内閣府発表で確認後に修正する前提でコメントを付す）。書式: `{ "2026-01-01": "元日", ... }`。

### 5.4 行構造

- 部署見出し行（例:「制作」）。表示のみ、折りたたみなし（折りたたみはV2検討）。
- 担当者ヘッダ行: `名前（countText）emoji comment` を横並び表示 ＋ 行末に**新規案件追加「＋」ボタン**。
- 案件行: タイトル ＋ 日付グリッド上のバー。タイトルの前に絵文字「📝」（U+1F4DD）を常に表示する。行ラベル側の背景色は「6.3」参照。
- `hidden: true` の案件行は通常非表示。上部の「非表示を表示」トグルONで半透明表示され、ポップアップから非表示解除できる（※データが見えなくなる袋小路を防ぐため、このトグルはV1に含める）。

### 5.5 バー（二軸表現）

- 1案件行に複数バー可。**文字ラベル = 工程**（修正は「修正3」のように番号連結）、**背景色 = 状態**（6色、「6.」参照）。
- **文字ラベル工程はバー（背景色）を描かず、文字ラベルのみを表示する**。文字色は `markColor` により「工程ごとの既定色（6.2 参照）」か「白」のいずれか。状態の色は反映しない。ただし **MT のみ文字ラベルを描かず、セル背景で表す**（5.12 参照。`markColor` は使用しない）。
- 文字ラベル工程は常に1日幅（`end = start + 1`）。
- `clsCheck: true` のバーは**囲い点線**を表示する（CL&S確認中）。点線の色はバーの背景色より濃い色にする（「6.」参照）。囲い点線は色付きバーを持つ工程（初校・修正・ラフ）でのみ表示する。
- ラベルがバー幅に収まらない場合ははみ出し表示を許容（見た目の調整はV2）。
- バー同士の期間重複は許可。V1では開始日順に描画し、重なり部は後のバーを前面にする（段積み表示はV2で検討）。
- クリック挙動: バー（MT のセルを含む）→ 5.11 の個別ポップアップ / 行ラベル → 5.6 の案件ポップアップ / 案件行の空白セル → 5.11 を**新規作成モード**で開く（そのセルの日付を開始日とする1日幅の下書き。［このバーを追加］を押すまでバーは作成されない。詳細は 5.11）。ドラッグ移動・伸縮は 5.10 のとおり（実装済み）。
  - 「空白セル」とは、その案件行のその日にいかなるバーの描画も無いセルを指す。文字ラベル工程（MT のセル背景を含む）が置かれた日のセルは空白ではない。

### 5.6 編集ポップアップ（案件単位）

行ラベル（📝タイトル部分）のクリックで開く（バーのクリックは 5.11、案件行の空白セルのクリックは 5.11 の新規作成モード）。**変更は即時反映＋自動保存**（明示的な保存ボタンは置かない。閉じるボタンのみ）。

- タイトル（テキスト入力）
- 制作メモ（`note`。1行テキスト入力。自由記入。例: ボリューム・サイズ・印刷会社。この画面でのみ表示し、グリッド・5.11・他画面には出さない）
- 担当者（プルダウンで1名選択。変更するとその担当者の下へ行が移動する）
- バー一覧（各行）: 工程セレクト（「修正」選択時のみ番号1〜20のセレクトを表示）/ 状態パレット（6色ボタン）/ 囲い点線チェックボックス（CL&S確認中）/ 開始日・終了日（`<input type="date">` のネイティブカレンダーピッカーを使用。独自ミニカレンダーは作らない）/ バー削除
  - 工程が文字ラベル工程のときは、状態パレットと囲い点線チェックボックスの代わりに**文字色の2択（既定色 / 白）**を出す（状態と囲い点線は画面に出ないだけで値は保持する）。終了日は編集不可。MT のときは文字色2択も表示しない（値は保持）。5.11 も同様。
  - ［バーを追加］直後は、追加された行へ自動スクロールし、ポップアップを閉じるまでその行を強調表示する（左端の青系ライン＋淡い背景。データには何も記録しない）。
- ［バーを追加］: 初期値 = 工程「ラフ」・状態「未着手」・囲い点線なし・期間「今日1日」
- ［完了・非表示］トグル（案件行単位で `hidden` を切替）
- ボタンの配置: 下部に左から［完了・非表示］［案件を削除］（赤系の危険色。誤操作防止のため［完了・非表示］との間に間隔を空ける。確認ダイアログ必須のまま）、右端に［閉じる］。
- 上部の見出し帯（タイトル文字の部分。ボタンの上を除く）をクリック＆ドラッグすると、ポップアップを画面内の任意の位置に移動できる。移動範囲は画面内に収まるよう制限する。閉じて再度開くと中央表示に戻る（位置は記憶しない）。

### 5.7 新規案件の追加

担当者ヘッダの「＋」→ その担当者に割当済み・タイトル空・バー1本（ラフ/未着手/今日1日）の新規案件が作られ、ポップアップが開く。

### 5.8 設定モーダル

- 部署: 追加 / 改名 / 削除（所属担当者が0のときのみ削除可）。
- 担当者: 追加 / 改名 / 絵文字 / 一言コメント / 案件数テキスト / 所属部署変更 / 削除。
  - 絵文字は自由入力ではなく、下記18種から選ぶ（未選択＝空も可）。並び順はこのとおり:
    😀 😁 😅 🙂 😐 😴 🫩 🤢 😵 🤠 😮 🥹 😢 😱 🥱 😷 😭 😤
  - 削除制約: 担当する案件が1件でも存在する担当者は削除不可（メッセージ表示。先に案件の担当者を変更するか、案件を削除する）。
- 社休日: 日付入力（`<input type="date">`）＋［追加］で登録。登録済み一覧を日付昇順で「YYYY-MM-DD（曜）」表示し、各行に［削除］。重複した日付の追加は無視する。過去日も登録・保持できる。変更は即時反映＋自動保存。
- 担当者ヘッダの3項目（案件数・絵文字・一言）は 5.13 の直接編集でも変更できる。名前の変更・担当者の追加削除・所属変更は本モーダルで行う。

### 5.9 保存・入出力

- localStorageキー: データ本体 `sgantt.data` / 表示状態 `sgantt.view`。すべての変更操作後に自動保存。
- ［書き出し］: `sgantt-backup-YYYYMMDD-HHMM.json` をダウンロード。
- ［読み込み］: ファイル選択 → `migrate()` → 「現在のデータをすべて置き換えます」確認 → 全置換。

### 5.10 バーのドラッグ操作（ロードマップV2-a/bで実装済み）

- 対象: すべてのバー。色付きバー（初校・修正・ラフ）は移動と伸縮、文字ラベル工程は移動のみ（常に1日幅を維持）。
- 移動: バー本体をドラッグすると日単位スナップで水平移動（長さ維持）。行をまたぐ縦移動はしない（担当・案件は変わらない）。
- 伸縮: 色付きバーの左右端 約6px を掴みしろとし、ホバーで左右矢印カーソルに変える。端のドラッグで開始日または終了日を変更。最小幅1日。開始と終了が交差する操作は1日幅でクランプ。
  - 掴みしろの縮退: バーの描画幅が 24px 未満のときは掴みしろを左右各「バー幅の1/4」に縮小し、12px 未満のときは掴みしろを置かない（伸縮不可。移動とクリックのみ。伸縮が必要な場合はポップアップの日付編集で行う）。期間表示の日数が多く列幅が狭いとき、掴みしろがバー本体を覆い尽くすのを防ぐため。
- クリックとの判別: pointerdown 後 4px 以上動いたらドラッグ、未満はクリック（ポップアップを開く）。
- ドラッグ中はバーを追従表示し、pointerup で確定・自動保存。Esc キーで取り消し（元の位置に戻す）。
- 表示期間の外へはドラッグできない（範囲外への移動はポップアップの日付編集で行う）。画面端の自動スクロールは実装しない。
- pointer イベントで実装する（タッチでも概ね動作するが、スマホ最適化は対象外のまま）。

### 5.11 工程バー個別ポップアップ（ロードマップV2-cで実装済み）

- 開き方: バー（文字ラベル工程・MT のセルを含む）のクリックで、そのバー1本だけを編集する小型ポップアップを開く。案件行の空白セルのクリックでは**新規作成モード**で開く（5.5 参照）。案件全体の編集は行ラベル側（📝タイトル部分）のクリックで従来どおり 5.6 のポップアップを開く。
- 新規作成モード: 空白セルのクリックで開く下書き状態。見出し帯に「新規バー」と表示して編集モードと区別する。フィールドは通常の 5.11 と同じで、初期値は 工程「ラフ」・状態「未着手」・囲い点線なし・クリックしたセルの日付1日。**確定前はデータに何も作成せず、グリッドにも描画しない**（「変更は即時反映」の唯一の例外）。フッターには［このバーを削除］の代わりに**［このバーを追加］**を表示し、押下した時点で初めてバーを作成・保存し、ポップアップを閉じる。［閉じる］または Esc で破棄（何も作られない）。［案件全体を編集…］は下書きを破棄して 5.6 を開く。
- 内容: 工程セレクト（「修正」選択時のみ番号1〜20）/ 状態パレット＋囲い点線チェック（色付きバー工程のとき）または文字色2択（文字ラベル工程のとき）/ 開始日・終了日（文字ラベル工程は開始日のみ）/ ［このバーを削除］（確認ダイアログ）/ ［案件全体を編集…］（5.6 のポップアップに切替）/ 閉じる。
- 変更は即時反映＋自動保存。見出し帯のドラッグ移動は 5.6 と同仕様。初期表示位置はクリックしたバーの近く（画面内に収まるよう調整）。
- 文字ラベル工程のクリック領域は、ラベルの矩形に加え、その1日セルのうち何も描かれていない部分を含む（クリック → 5.11、そこからのドラッグ → 移動）。同じセルに色付きバーが重なる場合、色付きバーの描画部分はその色付きバーのクリック領域のまま（原則: 見えているものが押せる。ただしラベル文字の矩形は従来どおり文字ラベル工程を優先）。同じセルに文字ラベル工程が複数あるときは前面のものを優先。これにより、文字ラベル工程のある日のセル内クリックで新規バーが生まれることはない。
- ［案件全体を編集…］で切り替えるときは 5.11 を閉じ、5.6 を通常どおり中央に表示する。
- クリック挙動は「バー（MT のセルを含む）→ 5.11 / 行ラベル → 5.6 / 案件行の空白セル → 5.11（新規作成モード）」（5.5 も参照）。

### 5.12 日ハイライト（TW・有休・MT）

- ハイライトは二層ある。
  - **【担当者単位（TW・有休）】** ある担当者の案件行のいずれかに工程 TW または有休のバーが置かれている日は、その担当者の**すべての案件行**の該当日セルに背景色を敷く（色は「6.4」参照）。担当者ヘッダ行・部署見出し行・日付ヘッダは塗らない。
  - **【セル単位（MT）】** 工程 MT のバーは文字ラベルを描かず、そのバーが属する案件行の該当日セルに背景色（「6.4」参照）を敷く。MT のセルはバーとしてのクリック（5.11）とドラッグ移動の当たり判定を維持する。同セルに他のバーが重なる場合は他のバーを前面に描く。MT のセルにポインタを乗せている間、ネイティブツールチップで「MT（打ち合わせ）」を表示する（`title` 属性。常時表示はしない）。
- いずれもバーから導出する表示であり、専用のデータは持たない。追加・移動・削除に自動で追従する。
- セル背景は重ね塗りせず1色のみ。優先順位: MT ＞ 有休 ＞ TW ＞ 今日（黄）＞ 土日祝・社休日の列色。
- 同じ日に同種のバーが複数あっても濃くしない。TW と有休が同日にあるときは有休を優先する。
- 非表示（`hidden`）の案件行に置かれたバーは、「非表示を表示」トグルが OFF のとき TW・有休の判定に含めず、MT のセル塗りも行わない（ON で半透明表示中は含める）。

### 5.13 担当者ヘッダの直接編集（ロードマップV2-cで実装済み）

- 対象は 案件数（`countText`）・絵文字（`emoji`）・一言コメント（`comment`）の3項目。名前の変更、担当者の追加・削除、所属部署の変更は従来どおり設定モーダル（5.8）で行う。
- `countText`・`comment`: クリックするとその場でテキスト入力欄に変わる。Enter または欄外クリックで確定、Esc で取り消し。確定時に即時保存。入力欄はおおむね表示位置のまま置き換え、レイアウトが大きく動かないようにする。
- `emoji`: クリックすると直下に選択パネル（5.8 の18種＋「なし」）を表示。選択で確定・即閉じ、パネル外クリックで取り消し。
- 空の項目のプレースホルダー（`countText`:「数」/ `emoji`:「◯」/ `comment`:「一言」）は常時表示しない。担当者ヘッダ行にポインタが乗っている間だけ薄いグレーで表示し、クリックすれば従来どおり編集を開始できる。レイアウトが動かないよう、空項目のクリック領域自体は常に確保し、文字の表示だけをホバーで切り替える（短いフェード可）。ホバーの無いタッチ環境では、空項目の入力は設定モーダル（5.8）で行う。
- 新規案件追加の「＋」ボタンとクリック領域を重ねない。

## 6. 状態カラーパレット（初期値・調整可）

実画面を見てesが調整する前提の仮置き。CSSカスタムプロパティで一元管理すること。

### 6.1 状態の色（バーの背景）

| 状態 | 色（仮） | 囲い点線の色（濃いめ） | 意図 |
|---|---|---|---|
| 未着手 | #e05252（赤） | #9e2f2f | 未着手が目立つこと自体を警告代わりにする |
| 制作中 | #6b9bdd（青） | #26518f | 作業中 |
| 校了 | #9aa0a6（グレー） | #5b6167 | 視覚的に沈める |
| 25% | #b7d98b（薄緑） | #6f9a45 | 進捗の淡→濃 |
| 50% | #82c26e | #4a8a37 | |
| 75% | #4f9e50 | #2c6b2d | |

バー上の文字色は次のとおり（esの指定）。

| 状態 | 文字色 |
|---|---|
| 未着手 / 制作中 / 75% / 校了 | 白 |
| 25% / 50% | 濃い緑系 |

### 6.2 文字ラベル工程の文字色（バー無し）

`markColor` が `"default"` のとき下表の色、`"white"` のとき白。

| 工程 | 既定の文字色（仮） |
|---|---|
| 納品 | #6b7176（グレー） |
| 入稿 | #c0392b（赤） |
| TW | #7cb87c（薄い緑・仮） |
| 有休 | #e08a8a（うすい赤・仮） |

### 6.3 行の背景色

| 行 | 背景色 |
|---|---|
| 部署見出し行 | #eef0f2 |
| 担当者ヘッダ行 | #fafbfc |
| 案件行（行ラベル側） | #f3f5f4 |

### 6.4 日ハイライト色

| 種別 | 背景色 |
|---|---|
| MT | #ffd966（濃い黄・仮。今日のうすい黄より明確に濃くする） |
| TW | #e6f2e6（薄緑・仮） |
| 有休 | 日曜・祝日の列背景色と同一（CSS カスタムプロパティを共通参照し、常に一致させる） |

※TW・有休は土日祝の列色と同程度の淡さに揃える。MT は当日の予定として目立つよう、意図的に濃くする。

## 7. ファイル構成

```
/index.html
/css/style.css
/js/holidays.js    … 祝日定数（2026・2027）
/js/store.js       … データモデル・CRUD・日付変換・保存・入出力・migrate（DOM非依存で書く）
/js/render.js      … グリッド・行・バーの描画
/js/popup.js       … 編集ポップアップ・設定モーダル
/js/app.js         … 初期化・イベント結線
/tests/store-test.js … Node.jsで実行するデータ層テスト
/docs/checklist.md   … es用の目視チェックリスト（Claude Codeが機能追加のたびに追記）
/docs/worklog.md     … セッションをまたぐ作業メモ（到達点・未解決の課題やバグと再現手順・次にやること）。仕様は書かない。
/CLAUDE.md           … 本ファイル
```

`store.js` はブラウザ・Node両対応で書き、`node tests/store-test.js` が単体で通ること（日付変換・CRUD・migrate・`assigneeId` 必須の検証などを検証）。

## 8. ロードマップと完了条件

| 段階 | 内容 | 完了条件 |
|---|---|---|
| **V0** | リポジトリ疎通 | clone → CLAUDE.md配置 → 仮の`index.html`（タイトルのみ）→ commit → push → GitHub PagesのURLで表示確認 |
| **V1-a** | 骨格とカレンダー | 期間コントロール（5.2）、日付ヘッダ（5.3）、今日・土日祝の色分け、表示状態の保存復元 |
| **V1-b** | データ層 | `store.js` 一式と `store-test.js` が全項目パス |
| **V1-c** | 表示 | 3階層の行構造（5.4）、バー描画の二軸表現（5.5）、非表示トグル |
| **V1-d** | 編集 | ポップアップ（5.6）、新規追加（5.7）、設定モーダル（5.8）、JSON入出力（5.9） |
| **V1完了** | — | esが実案件を登録し、1週間実運用できる |
| **V2-a** | ドラッグ移動 | 5.10 のうち移動（全バー種）。クリック判定・Escキャンセル含む |
| **V2-b** | 端ドラッグ伸縮 | 5.10 のうち伸縮（色付きバーのみ） |
| **V2-c** | クリック挙動の再編 | 5.11 の工程バー個別ポップアップ新設、行ラベル・空白部分クリック→案件ポップアップ、担当者ヘッダの直接編集（5.13） |
| **V2-d** | 並びと磨き込み | 並び替えUI（部署・担当者・案件、▲▼ボタン方式）、バー重なり時の段積み表示の要否判断、部署折りたたみの要否判断、色・余白・スマホ閲覧の調整。完了条件: 1週間以上の実運用の気づきを反映し、esが日常の道具になったと判断できること |
| **V3** | 共有 | Cloudflare Workers ＋（Upstash Redis または D1）による同期、共通パスコード認証、案件単位の書き込みで後勝ち消失を回避。詳細仕様はV2完了後に本ファイルへ追記 |

- 実装依頼は原則この段階単位で行う。段階をまたぐ先回り実装はしない。

## 9. 開発ルール（Claude Codeへの恒常指示）

1. 応答・コミットメッセージ・コード内コメントは**日本語**。esはデザイナーであり職業プログラマーではない。専門用語には短い説明を添える。
2. 1回の依頼 = 1つのまとまり。**常に動く状態でコミット**する。コミットメッセージ例:「V1-a: 日付ヘッダと祝日色分けを実装」。
3. 各変更後の検証（必須・この順で）:
   (1) 構文確認（`node --check` 相当）
   (2) `node tests/store-test.js` の実行（store変更の有無にかかわらず回帰確認）
   (3) 可能ならプレビューで表示確認し、`docs/checklist.md` の該当項目を案内
4. push前に、変更内容の要約を日本語でesに報告し、承認を得てからpushする。
5. 内部IDを画面に表示しない。外部ライブラリ・CDN・ビルド工程を導入しない（2.参照）。
6. データ構造変更時は `dataVersion` とmigrate必須（4.3参照）。**esの既存データを壊す変更を黙って行わない**。
7. gitはmain直コミットでよい（1人開発のため）。ブランチ運用はV3で再検討。
8. 仕様の解釈に迷ったら、実装せずまず質問する。
9. 配色・余白・文字サイズなど見た目の微調整は、esの指示があればコード先行で変更してよい。ただしその場合、同じ作業の中で本ファイルの該当箇所と変更履歴を必ず追随更新し、コードと仕様書を一致させた状態でコミットすること。（機能・データ構造・操作フローに関わる変更は従来どおり仕様書が先）
10. セッションの終了前・コンテキスト圧縮（/compact）の前には、`docs/worklog.md` を最新化する。内容は「ロードマップ上の到達点（1〜2行）/ 未解決の課題・バグと再現手順 / 次にやること」の3点のみ。仕様・決定事項は従来どおり本ファイルにのみ書き、worklog には書かない（二重管理の禁止）。
11. 作業開始時は、本ファイルとあわせて `docs/worklog.md` を読むこと。

## 10. やらないこと（V3完了まで提案も不要）

作業時間の集計行 / 未着手アラート / 未割当セクション / TODAYジャンプ / 午前午後の表示切替（※内部データは半日単位で保持するが、UIには出さない）/ 通知機能 / ユーザー別権限 / 印刷最適化 / 多言語対応 / ガント系ライブラリの導入

## 11. 変更履歴

※本ファイルの版数（v1.0、v2.3 など）とロードマップの段階（V0〜V3）は別物である。段階を指すときは「ロードマップV2」のように呼ぶ。

- v1.0（2026-08-04）: 初版。ブラウザ版Claudeとの仕様確定内容を反映。
- v1.1（2026-08-04）: 5.3 土曜の配色を赤系から水色系に変更（実装済みコードに仕様を追随）。あわせて 9. に見た目の微調整に関する運用ルールを追加。
- v1.2（2026-08-04）: 5.2 ［7日］の開始日を「今日の7日前」から「今日」に変更。5.3 に列幅の伸長規則を追記。行ラベル列の幅を拡大。仕様に無いまま仮置きしていた要素（未実装の操作ボタン・祝日名ツールチップ・期間の幅表示）を削除。
- v1.3（2026-08-04）: 5.1 上部に状態カラーの凡例を追加。
- v2.0（2026-08-05）: V1の実運用を受けた見直し。**dataVersion を 2 に更新**。
  - 3. 工程を6種に変更（初校 / 修正 / ラフ / MT / 入稿 / 納品）。「再校」→「修正」に名称変更、「MT（打ち合わせ）」を追加。
  - 3. 状態を6種に変更（未着手 / 制作中 / 校了 / 25% / 50% / 75%）。「完了」→「校了」に名称変更、「CL確認中」は廃止し囲い点線に置き換え。
  - 3./5.5 MT・入稿・納品はバーを描かず文字ラベルのみ表示（工程ごとに文字色を固定）。
  - 4.2/4.3 バーに `clsCheck`（囲い点線＝CL&S確認中）を追加。旧「CL確認中」は「制作中＋囲い点線」へ自動変換。
  - 5.1 凡例に囲い点線の見本を追加。
  - 5.6 囲い点線チェックボックスを追加。［案件を削除］を右上、［閉じる］を下部に移動。
  - 6. パレットを6色に整理し、囲い点線の色と、バー無し工程の文字色を追加。
  - 8. V2 に「個別の工程バー編集ポップアップ」を追記。
- v2.1（2026-08-05）: 実画面を見ての調整。**dataVersion を 3 に更新**。
  - 6.1 制作中の青を #4a7fd4 → #6b9bdd に。校了の文字色を白に。
  - 6.3 を新設し、案件行（行ラベル側）の背景色 #f3f5f4 を明記。
  - 5.4 案件名の前に絵文字「📝」（U+1F4DD）を常に表示。
  - 3./4.2/4.3/5.5/5.6/6.2 バーに `markColor`（既定色 / 白）を追加。MT・入稿・納品の文字色を選べるようにした。
- v2.2（2026-08-05）: 5.6 編集ポップアップの上部見出し帯をクリック＆ドラッグで移動できるようにした（データ構造の変更なし）。
- v2.3（2026-08-05）: 5.8 担当者の絵文字を自由入力から15種の選択式に変更（4.3を追随更新。データ構造の変更なし。emojiは引き続き文字列）。
- v2.4（2026-08-05）: ロードマップV2の詳細仕様を追加（5.10 ドラッグ操作、5.11 工程バー個別ポップアップ）。V2 を V2-a〜d に分割し、V1完了条件（1週間実運用）の実質を V2-d の完了条件へ引き継ぎ。タイトル行の版数表記を廃止し、版数とロードマップ段階の混同防止の注記を追加。
- v2.5（2026-08-05）: 5.10 に細いバーでの掴みしろ縮退規則を追加（ロードマップV2-b着手前の仕様補強）。5.4 の誤字修正。
- v2.6（2026-08-05）: ロードマップV2-b（端ドラッグ伸縮）の実装完了にあわせて記述を現在化。5.5 のバークリック／ドラッグの記述を更新し、5.10 の見出しを「実装済み」に変更。
- v2.7（2026-08-06）: 担当者を単一化。**dataVersion を 4 に更新**。projects の `assigneeIds`（配列）を `assigneeId`（1名）に変更。migrate(v3→v4): 担当1名の案件はそのまま引き継ぎ、担当2名以上の案件は人数分の独立した案件に分割（title・hidden・bars を複製し、案件・バーとも新しいIDを付与。以後は互いに独立）。想定外の担当0名は members 先頭に割当ててデータを保全する。5.6 の担当者欄をプルダウン（1名）に変更。3./4.2/4.3/5.4/5.8 を追随更新。
- v2.8（2026-08-06）: 工程に TW（テレワーク）と有休（有給休暇）を追加し8種に。両者は文字ラベル工程（常に1日幅・バー背景なし・markColor対応・ドラッグは移動のみ）。総称「文字ラベル工程」を用語集に定義し、各節の列挙を総称に置き換え。6.2 に両者の既定文字色を追加。5.6 のボタン配置を変更（［案件を削除］を右上から下部［完了・非表示］の右隣へ移動、赤系の危険色・間隔・確認ダイアログ維持）。dataVersion は 4 のまま（stage の許容値追加のみで構造変更・データ移行が無いため。migrate 不要）。
- v2.9（2026-08-06）: 5.12 TW・有休の日ハイライトを新設（担当者単位・バーからの導出表示。データ変更なし・dataVersion 4 のまま）。6.4 に背景色を追加。
- v2.10（2026-08-06）: ロードマップV2-c着手前の仕様補強。5.13（担当者ヘッダの直接編集）を新設。5.11 に空白部分クリックの扱い・文字ラベル工程のクリック領域・切替時の表示を追記。
- v2.11（2026-08-06）: ロードマップV2-c（クリック挙動の再編・工程バー個別ポップアップ・担当者ヘッダの直接編集）の実装完了にあわせて記述を現在化。5.5 のクリック挙動、5.6 の開き方、5.8 の担当者ヘッダ編集の記述を更新し、5.11・5.13 の見出しを「実装済み」に変更。
- v2.12（2026-08-06）: `docs/worklog.md` を新設し、セッション運用ルール（9章 10・11）を追加。仕様と決定は CLAUDE.md、作業状態は worklog という分担を明文化。7章の文言を追随修正。
- v2.13（2026-08-17）: 実運用10日超の気づき10点を反映（V2-d 第1弾）。①新規バー追加時の自動スクロールと強調表示 ②案件に制作メモ `note` を追加（**dataVersion を 5 に更新**。migrate(v4→v5): 全案件に note:"" を付与）③空白セルクリックでその日を開始日とする新規バー作成＋5.11 表示（v2.10 の「空白→5.6」を上書き）④有休ハイライト色を日祝の列色と共通変数化 ⑤絵文字を18種に拡張 ⑥5.13 のプレースホルダーを常時表示からヘッダ行ホバー時のみの表示に変更（クリック編集とクリック領域は維持）⑦［7日］を［7営業日］に変更（今日から営業日7日目まで表示）⑧書き出し・読み込みボタンを画面右端へ ⑨⑩ハイライトを二層化（MT をセル背景化・凡例に追加）し、優先順位を MT＞有休＞TW＞今日＞土日祝 に変更。
- v2.14（2026-08-17）: 文字ラベル工程のクリック領域をラベル矩形から1日セル全体（描画の無い部分を含む）に拡大し、「空白セル」の定義を明確化（v2.13③ の新規バー作成はバーの無いセルのみで発動。v2.10 のラベル矩形規則を上書き）。色付きバーとの重なりは「見えているものが押せる」を原則とする。MT セルにホバー時のネイティブツールチップを追加。
- v2.15（2026-08-17）: 実運用の気づき2点を反映。①空白セルクリックを即時作成から 5.11 の**新規作成モード**（下書き）に変更。［このバーを追加］押下で初めて作成（v2.13③ を上書き。誤クリックでのバー生成を解消。追加前に工程を選べるようになる）。②MT の日ハイライト色を #f3f5f4 から濃い黄 #ffd966（仮）に変更し、凡例の枠線を廃止（worklog 4. の es 確認結果を反映）。データ構造変更なし・dataVersion 5 のまま。
- v2.16（2026-08-18）: 社休日を追加。**dataVersion を 6 に更新**（migrate(v5→v6): トップレベルに companyHolidays: [] を付与）。設定モーダルに登録・削除UIを追加（5.8）。表示は日祝と同色の列色を共通変数で参照するが、日付ヘッダのセルは着色しない（5.3）。［7営業日］の営業日計算から社休日を除外（5.2）。
