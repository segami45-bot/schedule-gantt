# 目視チェックリスト

es が画面を見て確認するための項目です。機能を追加するたびに追記します。

確認は本番URL（<https://segami45-bot.github.io/schedule-gantt/>）で行ってください。
ローカルで開いた場合、入力したデータは本番には存在しません（CLAUDE.md 2-5）。

---

## V1-a: 骨格とカレンダー

### 期間コントロール（CLAUDE.md 5.2）

- [ ] 初めて開いたとき、開始日が「今日の7日前」・幅が「30日間」になっている
- [ ] ［7日］を押すと、**今日が一番左**に来て7日間になる
- [ ] ［7日］のとき、日付グリッドが画面の右端まで広がる（横スクロールが出ない）
- [ ] ［30日］を押すと、開始日が今日の7日前・幅が30日間になる
- [ ] ［30日］のように画面に収まらない日数のときは、列幅が細いまま横スクロールになる
- [ ] 開始日を直接変えると、その日から表示が始まる
- [ ] 終了日を直接変えると、幅がその日までに変わる
- [ ] 終了日に開始日より前の日を入れると、1日間に補正される（入力欄も直る）
- [ ] 120日を超える期間を入れると、120日に補正される（入力欄も直る）
- [ ] 期間を変えてページを再読み込みしても、同じ期間で開く

### 日付ヘッダ（CLAUDE.md 5.3）

- [ ] 各列に「月/日」と曜日（日〜土）が出ている
- [ ] 今日の列が黄色くなっている
- [ ] 日曜・祝日の列が薄い赤になり、日付の文字も赤い
- [ ] 土曜の列が薄い水色になり、日付の文字も青い
- [ ] 土曜が祝日の日は、水色ではなく赤になる
- [ ] 今日が土日祝と重なる日は、黄色（今日）が優先される
- [ ] 左の「担当者 / 案件」欄の幅が、案件名を書くのに足りている

### 祝日の確認（CLAUDE.md 5.3 / `js/holidays.js`）

2026年で赤くなるはずの日:

- [ ] 8/11（山の日）
- [ ] 9/21・9/22・9/23（敬老の日／国民の休日／秋分の日）… 土日と合わせて5連休
- [ ] 10/12（スポーツの日）
- [ ] 11/3（文化の日）／11/23（勤労感謝の日）

2027年は**暫定値**です。春分の日・秋分の日は前年2月の官報で正式決定されるため、
2026年2月の発表を確認して `js/holidays.js` の ★印の行を修正してください。

- [ ] 2027/3/21（春分の日・★暫定）／3/22（振替休日・★暫定）
- [ ] 2027/9/23（秋分の日・★暫定）

---

## V1-d（1回目）: 設定モーダルと JSON入出力

**ここから実際に登録できます。** 初回は［設定］から部署 → 担当者の順に登録してください。

### 設定モーダル（CLAUDE.md 5.8）

- [ ] ［設定］を押すとモーダルが開く
- [ ] ［閉じる］ボタンと Esc キーで閉じられる
- [ ] 部署を追加できる（入力欄で Enter でも追加できる）
- [ ] 部署名を書き換えると、その場でガント側の見出しも変わる
- [ ] 部署の右に所属人数が出ている
- [ ] 担当者を追加できる（名前＋所属部署を選ぶ）
- [ ] 部署が1つも無いあいだは、担当者を追加できない状態になっている
- [ ] 担当者の名前・案件数・絵文字・一言コメントをその場で書き換えられる
- [ ] 書き換えるとすぐガントの担当者ヘッダに反映される（保存ボタンは無い）
- [ ] 案件数・絵文字・コメントは空のままでもよい
- [ ] 担当者の所属部署を変えると、ガント側の並びと部署の人数表示が両方変わる

### 削除の制約（CLAUDE.md 5.8）

- [ ] 担当者がいる部署を削除しようとすると、赤いメッセージで拒否される
- [ ] 担当者が0人の部署は、確認ダイアログのあと削除できる
- [ ] 案件を担当している担当者を削除しようとすると、案件名入りのメッセージで拒否される（v2.7）
- [ ] 担当案件が0件の担当者は、確認のあと削除できる

### 書き出し（CLAUDE.md 5.9）

- [ ] ［書き出し］を押すと `sgantt-backup-YYYYMMDD-HHMM.json` がダウンロードされる
- [ ] ダウンロードしたファイルをテキストエディタで開くと中身が読める

### 読み込み（CLAUDE.md 5.9）

- [ ] ［読み込み］でファイルを選ぶと「すべて置き換えます」と件数が出る
- [ ] ［キャンセル］すると何も変わらない
- [ ] ［OK］すると内容が置き換わり、画面も切り替わる
- [ ] **書き出したファイルを読み込むと、元どおりになる**
- [ ] 関係ないファイル（例: 適当なテキスト）を選ぶと、理由が出て**いまのデータは残る**

---

## V1-d（2回目）: 編集ポップアップと新規案件追加

**これで V1 の機能がすべて揃いました。**

### 新規案件の追加（CLAUDE.md 5.7）

- [ ] 担当者ヘッダの行末に「＋」ボタンが出ている
- [ ] 「＋」を押すと、その担当者の案件が1件でき、編集ポップアップが開く
- [ ] できた案件はタイトル空・バー1本（ラフ／未着手／今日1日）になっている
- [ ] 「＋」を押しても、案件行のクリック（ポップアップ）が同時に起きない

### 編集ポップアップの開き方（CLAUDE.md 5.6）

- [ ] 左の案件タイトルをクリックすると開く
- [ ] バーをクリックすると開く
- [ ] 案件行の右側（バーの無い場所）をクリックしても開く
- [ ] 部署見出し・担当者ヘッダをクリックしても開かない

### ポップアップの中身（CLAUDE.md 5.6）

- [ ] タイトルを書き換えると、閉じなくてもガント側が変わる（保存ボタンは無い）
- [ ] 担当者がプルダウンで1名選べる（v2.7）
- [ ] バーの工程を選べる（工程の一覧は「v2.0」の節を参照）
- [ ] 状態パレットのボタンで色を変えられ、選択中の色に枠が付く
- [ ] 開始日・終了日がカレンダーから選べる
- [ ] ［バーを追加］でラフ／未着手／今日1日のバーが増える
- [ ] バーの［削除］でそのバーだけ消える
- [ ] ［完了・非表示］を押すと案件行が消える（［非表示を表示］で半透明で出る）
- [ ] 半透明の行をクリックしてポップアップを開き、非表示を解除できる
- [ ] ［案件を削除］は確認ダイアログが出て、キャンセルすると消えない

### V1 の仕上げ確認

- [ ] 実際の案件を数件登録し、［書き出し］でバックアップを取る
- [ ] ブラウザを閉じて開き直しても、登録した内容が残っている

---

## V1-b: データ層

**画面の見た目は変わりません。** データを作る・直す・保存する仕組みだけを作った段階です。
目視で確かめられるものが無いため、代わりにテストで検証しています。

es さんの確認は次の1つだけです。

- [ ] ターミナルで下のコマンドを実行し、「すべて通りました。」と出る

```bash
node tests/store-test.js
```

### テストで確認している内容（現在265項目）

| 分類 | 主な確認内容 |
|---|---|
| 日付変換 | CLAUDE.md 4.1 の例（2020-01-01午前=0 など）、1200日ぶんの往復、夏時間のある地域でもずれないこと |
| 部署 | 追加・改名・削除、担当者がいる部署は削除できないこと |
| 担当者 | 追加・改名・絵文字・コメント・部署異動・削除 |
| 案件 | 新規追加（タイトル空・バー1本・ラフ/未着手/今日1日）、非表示の切替 |
| 担当者と案件 | 案件の担当者は常に1名。付け替えで行が移動すること、担当案件がある担当者は削除できないこと |
| バー | 工程・状態・期間・囲い点線の変更、MT・入稿・納品が常に1日幅になること、一覧に無い工程・状態の拒否 |
| migrate | 省略された項目の補完、ずれたバーの丸め、壊れたJSON・矛盾したデータの拒否、旧版（dataVersion 1）からの変換 |
| 入出力 | 書き出し→読み込みで内容が戻ること、ファイル名 `sgantt-backup-YYYYMMDD-HHMM.json` |
| 読み込み失敗 | 壊れたファイルを読ませても、いま入っているデータが消えないこと |

### 補足

- テスト自体が正しく働くかを確認するため、`store.js` の規則をわざと5か所壊して実行し、
  すべてテストが失敗することを確認しています（確認後、元に戻してあります）。
- V1-b の時点では画面から呼び出していないため、**入力したデータは画面には出ません**。
  実際に触れるようになるのは V1-c（表示）・V1-d（編集）です。

---

## V1-c: 表示

**まだ画面から案件を登録できません**（登録は V1-d）。データが空のあいだは
左側に「部署と担当者がまだ登録されていません。」とだけ出るのが正しい状態です。

### 行の構造（CLAUDE.md 5.4）

- [ ] 部署見出し行 → 担当者ヘッダ行 → 案件行 の3階層で並んでいる
- [ ] 担当者ヘッダに「名前（案件数）絵文字 コメント」が横並びで出ている
- [ ] 案件数・絵文字・コメントが空の担当者は、その部分が出ない（詰めて表示される）
- [ ] 左のラベルと右のバーの行が、上下でぴったり揃っている
- [ ] 案件行に表示されるのはタイトルだけ（内部IDが出ていない）
- [ ] 長いタイトルは「…」で省略される

### バー（CLAUDE.md 5.5 / 6）

※ 工程・状態の名前は v2.0 で変わりました。最新の一覧は「v2.0」の節を参照してください。

- [ ] バーの文字が工程になっている
- [ ] 番号を持つ工程だけ「修正3」のように番号が付いている
- [ ] バーの色が状態を表している
- [ ] 期間が表示範囲をはみ出すバーは、画面の端で切れて表示される
- [ ] 表示範囲の外にしか無いバーは表示されない
- [ ] バーが重なったとき、開始日が後のバーが手前に出る
- [ ] **バーの文字が読めるか**（特に薄緑）

### 凡例（CLAUDE.md 5.1）

- [ ] 期間コントロールの下に「状態」の凡例が1行で出ている
- [ ] 色見本と状態名が並んでいる（一覧は「v2.0」の節を参照）
- [ ] 凡例の色が、実際のバーの色と同じ
- [ ] 画面幅を狭くすると凡例が2行に折り返され、崩れない

### 非表示（CLAUDE.md 5.4）

- [ ] 「完了・非表示」にした案件は通常は行ごと出ない
- [ ] ［非表示を表示］を押すと、その案件が半透明で出る
- [ ] ［非表示を表示］がONのあいだ、ボタンが押された見た目になる
- [ ] ページを再読み込みしても、ONにしていた状態が保たれる

---

## v2.0: 工程・状態の見直しと囲い点線

**この変更で保存形式が `dataVersion 2` に上がりました。**
以前に書き出したバックアップ（dataVersion 1）を読み込むと、
「再校→修正」「完了→校了」「CL確認中→制作中＋囲い点線」に自動変換され、
変換した内容が読み込み後のメッセージに一覧で出ます。

### 工程（CLAUDE.md 3）

- [ ] ポップアップの工程セレクトが「初校 / 修正 / ラフ / MT / 入稿 / 納品」の順で並んでいる
- [ ] 「修正」を選んだときだけ番号（1〜20）のセレクトが出る（旧「再校」の位置づけ）
- [ ] 「MT」「入稿」「納品」を選ぶと、色パレットと「確認中」チェックが消え、
      代わりに「文字のみ・1日」と文字色の2択が出る（v2.1）
- [ ] 「MT」「入稿」「納品」は終了日が灰色（1日固定）

### バーの見た目（CLAUDE.md 5.5 / 6）

- [ ] MT・入稿・納品はガント上でバー（色の帯）が無く、文字だけが出ている
- [ ] MT と納品の文字がグレー、入稿の文字が赤
- [ ] 初校・修正・ラフは今までどおり色付きのバー
- [ ] **6色が実際に見分けられるか**（調整は `css/style.css` の `--bar-◯◯-bg`）

### 囲い点線＝CL&S確認中（CLAUDE.md 5.5 / 5.6）

- [ ] バー行の「確認中」にチェックを入れると、そのバーに点線の囲いが付く
- [ ] 点線の色がバーの色より濃く、見分けられる
- [ ] 状態（色）を変えても点線は消えない
- [ ] 凡例の右端に、区切り線のあとで「CL&S確認中」の点線見本が出ている
      （見本は塗りなし・グレーの点線。実際のバーの点線は状態ごとの濃い色）
- [ ] **点線の太さ・濃さが実際に見て分かりやすいか**（調整は `--bar-◯◯-ln`）

### ポップアップのボタン配置（CLAUDE.md 5.6）

- [ ] ボタンの配置は v2.8 で変わりました（下の「v2.8」の節を参照）
- [ ] 誤って［案件を削除］を押しても、確認ダイアログでキャンセルできる

### 旧データの読み込み（CLAUDE.md 4.3）

- [ ] v2.0 より前に書き出したバックアップがあれば、それを読み込んでみる
- [ ] 読み込み後のメッセージに「再校→修正」などの変換内容が出る
- [ ] 変換後のバーの日付・番号が元のままになっている

---

## v2.1: 配色と案件名の調整、文字色の選択

**保存形式が `dataVersion 3` に上がりました。**
v2.0 で書き出したバックアップもそのまま読み込めます（文字色は「既定の色」になります）。

### 配色（CLAUDE.md 6.1 / 6.3）

- [ ] 制作中の青が以前より薄くなっている（#6b9bdd）
- [ ] 校了（グレー）のバーの文字が白になっている
- [ ] 案件名の行（左のラベル側）の背景が薄いグレー（#f3f5f4）になっている
- [ ] **薄くした青の上で白文字が読めるか**（読みにくければ `--bar-wip-bg` を調整）
- [ ] **グレーの上で白文字が読めるか**（読みにくければ `--bar-ok-fg` を調整）

### 案件名の絵文字（CLAUDE.md 5.4）

- [ ] 案件名の前に「📝」が付いている
- [ ] タイトルが空の案件でも「📝 (無題)」と出る

### MT・入稿・納品の文字色（CLAUDE.md 6.2）

- [ ] ポップアップで MT・入稿・納品を選ぶと、色の選択肢が2つ出る（既定の色 / 白）
- [ ] 「白」を選ぶと、ガント上の文字が白くなる
- [ ] **色付きバーに重なった入稿・納品が、白にすると読めるようになる**
- [ ] 工程を変えても、選んだ文字色が保たれる
- [ ] ⚠️ **白を選ぶと、バーに重なっていない場所では文字が見えなくなります**
      （背景が白のため。見えなくなったら「既定の色」に戻してください）

### 旧バックアップの読み込み

- [ ] v2.0 で書き出したファイルを読み込める（警告なしで通る）
- [ ] 読み込んだバーの番号・囲い点線がそのまま残っている

---

## v2.2: 編集ポップアップのドラッグ移動

### 案件の編集ポップアップ（CLAUDE.md 5.6）

- [ ] 「案件の編集」の見出し帯（タイトル文字のあたり）にマウスを乗せると、指カーソルではなく移動用のカーソルになる
- [ ] 見出し帯をクリック＆ドラッグすると、ポップアップが画面内の好きな位置に動く
- [ ] 画面の端まで動かしても、ポップアップが画面の外にはみ出さない
- [ ] 一度閉じて別の案件をクリックすると、画面中央に戻って開く（動かした位置は覚えていない）

### 設定モーダルは対象外

- [ ] ［設定］モーダルの見出し帯はドラッグしても動かない（今回はガント側の案件編集のみが対象です）

---

## v2.3: 担当者の絵文字を選択式に変更

### 設定モーダル（CLAUDE.md 5.8）

- [ ] 担当者の「絵文字」欄が、自由入力の枠から選択式（プルダウン）に変わっている
- [ ] 開くと「（なし）」＋15種の絵文字が並んでいる
- [ ] 選ぶとすぐ担当者ヘッダに反映される（保存ボタンは無い）
- [ ] 「（なし）」を選ぶと絵文字が消える
- [ ] 15種の絵文字が全部読み取れる（文字化けしていないか）

### 以前に絵文字を自由入力していた担当者がいる場合

- [ ] その担当者の欄を開くと、元の絵文字が一覧の先頭に追加された形でそのまま選ばれている
  （15種の中に無い絵文字でも、選び直すまでは消えません）

---

## V2-a: バーのドラッグ移動（CLAUDE.md 5.10）

**バーを掴んで動かせるようになりました。** 伸縮（端を引っぱって期間を変える）は次の V2-b です。

### 移動

- [ ] バーにマウスを乗せると、掴めることを示すカーソル（手の形）になる
- [ ] バーをドラッグすると、1日ずつぴったり動く（中途半端な位置で止まらない）
- [ ] バーの長さは変わらない
- [ ] 上下にドラッグしても、別の担当者・別の案件には移らない
- [ ] MT・入稿・納品の文字も同じようにドラッグで動かせる（1日のまま）
- [ ] 手を離した時点で保存される（保存ボタンは無い）

### クリックとの使い分け

- [ ] バーを「ただクリック」するとポップアップが開く（従来どおり）
- [ ] バーを「ドラッグ」したときはポップアップが開かない
- [ ] ほんの少し（4px 未満）動いてしまった場合はクリック扱いになる

### 取り消し

- [ ] ドラッグ中に Esc キーを押すと、元の位置に戻って保存されない

### 表示期間の制限

- [ ] 表示している期間の外へはドラッグできない（左端・右端で止まる）
- [ ] 期間の外へ動かしたいときは、ポップアップの日付欄で変更する
- [ ] 画面の端まで持っていっても、自動でスクロールはしない（仕様どおり）
- [ ] 表示期間からはみ出しているバーは、はみ出す向きへは動かせない
      （期間内へ戻す向きには動かせる）

---

## V2-b: バーの端ドラッグ伸縮（CLAUDE.md 5.10）

**バーの左右の端を引っぱって、期間を変えられるようになりました。**

### 伸縮

- [ ] 色付きバー（初校・修正・ラフ）の左右の端にマウスを乗せると、左右矢印のカーソルになる
- [ ] 右端を引っぱると、開始日はそのままで終了日だけが変わる
- [ ] 左端を引っぱると、終了日はそのままで開始日だけが変わる
- [ ] 1日ずつぴったり変わる
- [ ] 端から離れた「バーの真ん中あたり」を掴んだときは、これまでどおり移動になる
- [ ] 手を離した時点で保存される

### 最小幅と交差

- [ ] 右端を左へ大きく引っぱっても、1日分より短くならない
- [ ] 左端を右へ大きく引っぱっても、1日分より短くならない

### MT・入稿・納品

- [ ] MT・入稿・納品の端を掴んでも伸縮しない（移動になり、1日のまま）

### 表示期間の制限・取り消し

- [ ] 表示期間の外へは伸ばせない（左端・右端で止まる）
- [ ] 伸縮の途中で Esc キーを押すと、元の長さに戻って保存されない

### 細いバーでの掴みしろ（CLAUDE.md 5.10 掴みしろの縮退）

列幅は最低 42px あるため、**通常の使い方ではこの状態になりません**。
将来 `css/style.css` の `--col-width` を小さくしたときのための備えです。

- [ ] （参考）列幅を極端に狭めても、掴みしろがバー本体を覆い尽くさない

---

## v2.7: 担当者の単一化

**保存形式が `dataVersion 4` に上がりました。**
案件は「1件につき担当者1名」になりました。同じ実案件を複数人で分担する場合は、
**各担当者が自分の行として別々に登録**します（互いに独立し、連動しません）。

### 旧データの自動変換（重要）

以前のデータで**複数人を担当に設定していた案件は、人数分の別々の案件に分かれます**。
分かれた後は互いに独立するため、片方を直してももう片方は変わりません。

- [ ] 読み込み後のメッセージに「◯件の独立した案件に分けました」と出る
- [ ] 分かれた案件が、それぞれの担当者の下に1件ずつ出ている
- [ ] タイトル・バーの内容が両方に複製されている
- [ ] 片方のバーを動かしても、もう片方は動かない
- [ ] 片方を削除しても、もう片方は残る
- [ ] 担当1名だった案件は、そのまま変わらず残っている

### 編集ポップアップ（CLAUDE.md 5.6）

- [ ] 担当者欄がチェックボックスからプルダウン（1名選択）に変わっている
- [ ] プルダウンが部署ごとにまとまっている
- [ ] 担当者を変えると、案件行がその担当者の下へ**移動する**（増えない）

### 担当者の削除（CLAUDE.md 5.8）

- [ ] 案件を担当している担当者は削除できず、案件名入りのメッセージが出る
- [ ] 担当案件が0件の担当者は、確認のあと削除できる

---

## v2.8: 工程に TW・有休を追加、ボタン配置の変更

**保存形式は `dataVersion 4` のままです**（工程の選択肢が増えただけで、
データの持ち方は変わっていません）。以前のバックアップもそのまま読み込めます。

### 工程の追加（CLAUDE.md 3 / 6.2）

- [ ] ポップアップの工程セレクトが8種になっている
      （初校 / 修正 / ラフ / MT / 入稿 / 納品 / **TW** / **有休**）
- [ ] TW・有休を選ぶと、色パレットの代わりに文字色の2択が出る
- [ ] TW・有休は終了日が灰色（1日固定）
- [ ] ガント上で TW が薄い緑、有休がうすい赤の文字で出る（バーの帯は無い）
- [ ] TW・有休も「白」を選べる（色付きバーに重なったとき用）
- [ ] TW・有休のバーはドラッグで移動できる（端を掴んでも伸縮しない）
- [ ] **TW の薄い緑・有休のうすい赤が実際に見て読めるか**
      （調整は `css/style.css` の `--mark-tw` / `--mark-yukyu`）

### ボタン配置の変更（CLAUDE.md 5.6）

- [ ] 「案件の編集」の右上からボタンが無くなっている
- [ ] 下部に左から［完了・非表示］［案件を削除］が並び、右端に［閉じる］がある
- [ ] ［案件を削除］が赤い塗りになっている
- [ ] ［完了・非表示］と［案件を削除］の間に間隔が空いている
- [ ] ［案件を削除］を押すと確認ダイアログが出て、キャンセルすると消えない
- [ ] 見出し帯のドラッグ移動は今までどおり効く

---

## v2.9: TW・有休の日ハイライト

**保存形式は `dataVersion 4` のままです**（バーから毎回計算して表示するだけで、
専用のデータは持ちません）。

### 担当者単位のハイライト（CLAUDE.md 5.12）

- [ ] ある案件に TW のバーを置くと、**その担当者の案件行すべて**の該当日が薄緑になる
- [ ] 有休のバーを置くと、同じように該当日がうす赤になる
- [ ] 塗られるのは案件行だけ（担当者ヘッダ行・部署見出し行・日付ヘッダは塗られない）
- [ ] 別の担当者の行は塗られない
- [ ] バーを動かす・消すと、ハイライトもすぐ追従する

### 色の優先順位（CLAUDE.md 5.12）

- [ ] 今日の列は黄色のまま（TW・有休があっても黄色が優先）
- [ ] 土日祝の日に TW・有休があると、薄緑／うす赤が優先される
- [ ] 同じ日に TW と有休の両方があると、うす赤（有休）になる
- [ ] 同じ日に同じ種類のバーが2本あっても、色は濃くならない

### 非表示との関係（CLAUDE.md 5.12）

- [ ] ［非表示を表示］が OFF のとき、非表示にした案件の TW・有休は数えない
- [ ] ［非表示を表示］を ON にすると、その分も塗られるようになる

### 見え方の確認

- [ ] **薄緑・うす赤が、土日祝の色と見分けられるか**
      （調整は `css/style.css` の `--day-tw` / `--day-yukyu`）
- [ ] バーの色や文字が、ハイライトの上でも読めるか

---

## v2.13 (A): 磨き込み（ボタン配置・7営業日・絵文字18種・プレースホルダー・色の優先順位）

**保存形式は `dataVersion 4` のままです**（データの持ち方は変えていません）。

### 操作ボタンの配置（CLAUDE.md 5.1）

- [ ] ［設定］［非表示を表示］が期間コントロールの右隣にある
- [ ] ［書き出し］［読み込み］が画面の右端に離れて置かれている
- [ ] 画面幅を狭めても押せる（折り返して2段になってよい）

### ［7営業日］（CLAUDE.md 5.2）

- [ ] ボタンの文字が「7営業日」になっている
- [ ] 押すと今日が一番左に来る
- [ ] 今日が月〜木なら、幅がだいたい9日になる（土日を1回はさむ）
- [ ] 期間内に祝日があると、その分だけ幅が広がる（例: 9/21〜9/23 を含む週）
- [ ] 終了日の入力欄も、伸びた終了日に合わせて書き換わっている

### 絵文字18種（CLAUDE.md 5.8 / 5.13）

- [ ] 設定モーダルの絵文字プルダウンに 😷 😭 😤 が末尾に増えている
- [ ] 担当者ヘッダの絵文字をクリックして出るパネルにも3つ増えている
- [ ] 増えてもパネルが画面からはみ出さない

### プレースホルダーの表示（CLAUDE.md 5.13）

- [ ] 未入力の「数」「◯」「一言」が、ふだんは見えない
- [ ] 担当者ヘッダ行にマウスを乗せると、薄いグレーで出てくる
- [ ] 出たものをクリックすると、これまでどおり編集を始められる
- [ ] 出たり消えたりしても、名前や「＋」ボタンの位置がずれない

### 日ハイライトの優先順位（CLAUDE.md 5.12 / 6.4）

- [ ] **今日**の日に TW・有休を置くと、黄色ではなく薄緑／うす赤になる
- [ ] 有休の色が、日曜・祝日の列の色とぴったり同じになっている
- [ ] 土日祝の日に TW・有休があると、薄緑／うす赤が優先される

---

## v2.13 (B): 制作メモと、新規バーの強調表示

**保存形式が `dataVersion 5` に上がります**。初めて開いたときに自動で変換され
（全案件に空の制作メモが付くだけ）、以前のバックアップ（1〜4）もそのまま読み込めます。
新しく書き出したファイルは、この版より前の sgantt では読めません。

### 制作メモ（CLAUDE.md 4.3 / 5.6）

- [ ] 「案件の編集」のタイトルの下に［制作メモ］の入力欄がある
- [ ] 未入力のときは「ボリューム・サイズ・印刷会社など」と薄く出ている
- [ ] 入力して欄から離れると保存され、閉じて開き直しても残っている
- [ ] タイトルや担当者を変えても、制作メモは消えない
- [ ] 制作メモはガント本体（行ラベル・バー）には出ない
- [ ] 工程バーの個別ポップアップ（バーをクリック）にも出ない
- [ ] ［書き出し］で保存したJSONを［読み込み］すると、制作メモも戻る

### 保存形式の更新（dataVersion 5）

- [ ] 更新後に初めて開いたとき、案件・バーが以前のまま表示されている
- [ ] 更新前に書き出したバックアップを読み込んでも、そのまま開ける
- [ ] 読み込みの確認ダイアログに件数が出て、「自動で調整しました」の余計な報告は出ない

### 新規バーの自動スクロールと強調表示（CLAUDE.md 5.6）

- [ ] ［バーを追加］を押すと、増えた行に左端の青いラインと淡い青の背景が付く
- [ ] バーが多くて下に隠れる場合、追加した行まで自動でスクロールする
- [ ] 続けて2本追加すると、両方に強調表示が付く
- [ ] 強調表示された行も、工程・状態・日付をふつうに編集できる
- [ ] ポップアップを閉じて開き直すと、強調表示が消えている
- [ ] 強調表示のぶんだけ行の中身がずれて見えない（左のラインで幅が変わらない）

---

## v2.13 (C): 空白セルのクリックと、MT のセル背景表現

**保存形式は `dataVersion 5` のままです**（MT の見せ方が変わるだけで、データは変わりません）。

### 空白セルのクリックで新規バー（CLAUDE.md 5.5 / 5.11）

- [ ] 案件行の何も無いセルをクリックすると、その日1日のバー（ラフ・未着手）ができる
- [ ] できたバーの個別ポップアップが、クリックしたセルのすぐ近くに開く
- [ ] 日付欄がクリックした日になっている（開始＝終了）
- [ ] ［このバーを削除］で、押し間違えたバーを取り消せる
- [ ] 左の行ラベル（📝案件名）をクリックすると、これまでどおり「案件の編集」が開く
- [ ] バーをクリックすると、これまでどおり個別ポップアップが開く（バーは増えない）
- [ ] バーをドラッグして動かしたあと、余分なバーができていない
- [ ] 表示の端（左端・右端）のセルをクリックしても、その日のバーができる

### MT のセル背景（CLAUDE.md 5.12 / 6.4）

- [ ] MT の文字が消え、その日のセルがうすいグレーで塗られている
- [ ] MT のセルをクリックすると個別ポップアップが開く（セルのどこでも押せる）
- [ ] MT のセルはドラッグで別の日へ動かせる（1日幅のまま）
- [ ] MT の行に「セル背景・1日」と出て、文字色の2択は出ない
- [ ] MT に他のバー（ラフ・修正など）が重なると、そのバーが前面に出る
- [ ] TW・有休と同じ日に MT を置くと、MT のグレーが優先される
- [ ] 凡例の右端に MT の見本（グレー＋枠線）が出ている
- [ ] **うすいグレーが実際に見て分かるか**（調整は `css/style.css` の `--day-mt`）

### 気をつけて見てほしい点

- 入稿・納品・TW・有休の**文字から離れた枠内**のクリックで新しいバーができてしまう
  問題は、次の v2.14 (D) で解消しました（下の項目で確認してください）。

---

## v2.14 (D): 文字ラベル工程のクリック領域と、MT のツールチップ

**保存形式は `dataVersion 5` のままです**（押せる範囲が変わるだけで、データは変わりません）。

### 文字ラベル工程の枠内が押せる（CLAUDE.md 5.11）

- [ ] 入稿・納品・TW・有休の**文字から離れた枠内**をクリックしても、新しいバーができない
- [ ] そのかわり、そのバーの個別ポップアップが開く（工程と日付が合っている）
- [ ] 枠内のどこからでもドラッグして、別の日へ動かせる（1日幅のまま）
- [ ] ドラッグ中は文字も一緒に動く（当たり判定だけが動いて文字が取り残されない）
- [ ] ドラッグ中に Esc を押すと、文字も元の位置に戻る
- [ ] ドラッグしたあと、余分なバーができていない
- [ ] 枠にマウスを乗せると、手のかたち（掴めるカーソル）になる

### 色付きバーと重なったとき（CLAUDE.md 5.11「見えているものが押せる」）

- [ ] 色付きバー（ラフ・初校・修正）の**塗られている部分**をクリックすると、
      その色付きバーのポップアップが開く
- [ ] 同じ日に置いた入稿などの**文字の上**をクリックすると、
      文字ラベル工程のほうのポップアップが開く（文字は色付きバーより前面のまま）
- [ ] 重なった状態でも、それぞれをドラッグで動かせる
- [ ] 色付きバーのすぐ上・すぐ下のわずかな余白をクリックしても、新しいバーはできない
      （何も起きません。その日はもう空いていないため）

### バーの無いセル（CLAUDE.md 5.5 の「空白セル」）

- [ ] バーが1本も無いセルをクリックすると、これまでどおり新規バーができる
- [ ] 期間の左端・右端のセルでも作られる

### MT のツールチップ（CLAUDE.md 5.12）

- [ ] MT のセルにマウスを乗せて少し待つと「MT（打ち合わせ）」と出る
- [ ] 常時は何も文字が出ていない（セルの色だけ）

---

## v2.15 (A): 空白セルの新規作成モード

**保存形式は `dataVersion 5` のままです**（データの持ち方は変わっていません）。

### 下書きとして開く（CLAUDE.md 5.11 新規作成モード）

- [ ] バーの無いセルをクリックすると、見出しが「**新規バー**」のポップアップが開く
- [ ] 工程「ラフ」・状態「未着手」・囲いなし・クリックした日1日 になっている
- [ ] **この時点ではガントに何も出ていない**（バーは増えていない）
- [ ] フッターが［案件全体を編集…］［**このバーを追加**］［閉じる］になっている
      （［このバーを削除］は出ない）
- [ ] バーの行に［削除］ボタンが出ていない

### 追加する前に編集できる

- [ ] 工程を変えると、それに応じて色パレット／文字色／「セル背景・1日」が切り替わる
- [ ] 状態・囲い点線・日付も、追加する前に変えられる
- [ ] 「修正」を選ぶと番号のセレクトが出る
- [ ] 文字ラベル工程を選ぶと終了日が灰色になる
- [ ] どれを変えても、まだガントには何も出ない

### 確定と破棄

- [ ] ［このバーを追加］を押すと、その内容のバーができてポップアップが閉じる
- [ ] ［閉じる］を押すと、**何も作られない**
- [ ] Esc キーでも、**何も作られない**
- [ ] ［案件全体を編集…］を押すと下書きは捨てられ、「案件の編集」が中央に開く
      （バーは増えていない）
- [ ] 誤ってセルをクリックしても、閉じるだけで元の状態に戻る

### 従来どおりであること

- [ ] 既存のバーをクリックしたときは見出しが案件名で、
      フッターは［案件全体を編集…］［このバーを削除］［閉じる］
- [ ] 既存のバーの編集は、これまでどおりその場で保存される
- [ ] 「案件の編集」の［バーを追加］は、これまでどおり押した時点で1本増える

---

## v2.15 (B): MT の色

**保存形式は `dataVersion 5` のままです**（色だけの変更です）。

### MT の日ハイライト（CLAUDE.md 6.4）

- [ ] MT のセルが**濃い黄**（#ffd966）になり、離れて見てもすぐ分かる
- [ ] 今日の列のうすい黄（#fff4c2）と、はっきり見分けられる
- [ ] 今日に MT を置くと、MT の濃い黄が優先される
- [ ] TW・有休と同じ日に MT を置くと、MT の濃い黄が優先される
- [ ] MT に色付きバーが重なると、これまでどおり色付きバーが前面に出る
- [ ] MT のセルにマウスを乗せると「MT（打ち合わせ）」と出る

### 凡例（CLAUDE.md 5.1）

- [ ] 凡例の右端の MT の見本が、濃い黄の塗りだけになっている（枠線が無い）
- [ ] 見本の色が、実際の MT のセルの色と同じ
- [ ] **濃さがちょうどよいか**（調整は `css/style.css` の `--day-mt` の1か所）

---

## v2.16: 社休日（会社の休業日）

**保存形式が `dataVersion 6` に上がります**。初めて開いたときに自動で変換され
（空の社休日リストが付くだけ）、以前のバックアップ（1〜5）もそのまま読み込めます。
新しく書き出したファイルは、この版より前の sgantt では読めません。

### 登録と削除（CLAUDE.md 5.8）

- [ ] ［設定］の「担当者」の下に「社休日」のセクションがある
- [ ] 日付を選んで［社休日を追加］を押すと、一覧に「2026-08-20（木）」の形で増える
- [ ] 追加すると日付の入力欄が空に戻る
- [ ] 一覧が日付の**昇順**に並ぶ（あとから前の日付を足しても正しい位置に入る）
- [ ] 同じ日付をもう一度追加しても増えない（エラーも出ない）
- [ ] 過去の日付も登録できる
- [ ] 各行の［削除］で1件だけ消える（確認ダイアログは出ない）
- [ ] 追加・削除するとすぐガント側の色が変わる（設定を閉じなくてよい）
- [ ] 日付を選ばずに［社休日を追加］を押すと、赤いメッセージが出て何も起きない

### 列の色（CLAUDE.md 5.3）

- [ ] 社休日にした平日の列が、日曜・祝日と同じ薄い赤になる
- [ ] **日付ヘッダ（月/日と曜日）は色が変わらない**（暦の上では平日と分かる）
- [ ] 色が付くのは部署見出し行・担当者ヘッダ行・案件行のすべて
- [ ] 土曜を社休日にすると、水色ではなく薄い赤になる（ヘッダの曜日は水色のまま）
- [ ] 今日を社休日にすると、今日の黄色が優先される
- [ ] 社休日に TW・有休・MT があると、これまでどおりそちらの色が優先される
- [ ] 社休日の色が、日曜・祝日の色とぴったり同じ

### ［7営業日］（CLAUDE.md 5.2）

- [ ] 社休日を含む期間で［7営業日］を押すと、そのぶん表示幅が広がる
      （社休日が1日で10日、2日で11日）
- [ ] 社休日を消すと、元の9日に戻る
- [ ] 祝日と社休日が同じ日でも、二重に数えられない

### 保存形式の更新（dataVersion 6）

- [ ] 更新後に初めて開いたとき、案件・バー・担当者が以前のまま表示されている
- [ ] 社休日を1件登録して再読み込みすると、その社休日が残っている
- [ ] ［書き出し］したJSONに `companyHolidays` が入っている
- [ ] 書き出したファイルを［読み込み］すると、社休日も復元される
- [ ] 更新前に書き出したバックアップを読み込んでも、そのまま開ける（社休日は空になる）

---

## v2.17 (A): 並び替えUI

**保存形式は `dataVersion 6` のままです**（`order` の値が入れ替わるだけです）。

### 部署・担当者（CLAUDE.md 5.14 / 5.8）

- [ ] ［設定］の部署・担当者の各行に、［削除］の左に小さな▲▼が出ている
- [ ] ▲▼を押すと、その場で並びが変わり、ガント側もすぐ変わる
- [ ] いちばん上の行の▲、いちばん下の行の▼が薄く（押せない状態に）なっている
- [ ] 担当者は**同じ部署の中でだけ**動く（別の部署の人と入れ替わらない）
- [ ] 担当者の一覧が部署ごとにまとまって並んでいる（ガント側と同じ並び）
- [ ] 部署が1つだけ・担当者が1人だけのときは、▲▼が両方とも押せない
- [ ] 並び替えたあとページを再読み込みしても、その並びのまま

### 案件（CLAUDE.md 5.14）

- [ ] 案件名の行にマウスを乗せると、行の右端に小さな▲▼が出る
- [ ] マウスを外すと消える（ふだんは見えない）
- [ ] ▲▼が出ても、案件名の表示位置や幅が動かない
- [ ] ▲▼を押すと並びが入れ替わり、**案件の編集ポップアップは開かない**
- [ ] 同じ担当者の中でだけ動く（別の担当者の案件と入れ替わらない）
- [ ] その担当者の案件が1件だけのときは、▲▼が両方とも押せない
- [ ] いちばん上の案件の▲、いちばん下の案件の▼が押せない状態になっている
- [ ] ［非表示を表示］が OFF のとき、隠れている案件を飛び越えて入れ替わる
- [ ] 並び替えたあとページを再読み込みしても、その並びのまま
- [ ] 担当者を変えた案件は、移った先のいちばん下ではなく元の順番で入る
      （気になる場合は▲▼で調整）

### 触っておきたい確認

- [ ] 並び替えたあと［書き出し］→［読み込み］しても、その並びが保たれる
- [ ] ▲▼の大きさ・濃さが押しやすいか（`css/style.css` の `.movebtn`）
