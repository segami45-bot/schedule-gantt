# 目視チェックリスト

es が画面を見て確認するための項目です。機能を追加するたびに追記します。

確認は本番URL（<https://segami45-bot.github.io/schedule-gantt/>）で行ってください。
ローカルで開いた場合、入力したデータは本番には存在しません（CLAUDE.md 2-5）。

---

## V1-a: 骨格とカレンダー

### 期間コントロール（CLAUDE.md 5.2）

- [ ] 初めて開いたとき、開始日が「今日の7日前」・幅が「30日間」になっている
- [ ] ［7日］を押すと、**今日が一番左**に来て7日間になる
- [ ] ［7日］のとき、日付グリッドが画面の右端まで広がる（横スクロールが出ない）
- [ ] ［30日］を押すと、開始日が今日の7日前・幅が30日間になる
- [ ] ［30日］のように画面に収まらない日数のときは、列幅が細いまま横スクロールになる
- [ ] 開始日を直接変えると、その日から表示が始まる
- [ ] 終了日を直接変えると、幅がその日までに変わる
- [ ] 終了日に開始日より前の日を入れると、1日間に補正される（入力欄も直る）
- [ ] 120日を超える期間を入れると、120日に補正される（入力欄も直る）
- [ ] 期間を変えてページを再読み込みしても、同じ期間で開く

### 日付ヘッダ（CLAUDE.md 5.3）

- [ ] 各列に「月/日」と曜日（日〜土）が出ている
- [ ] 今日の列が黄色くなっている
- [ ] 日曜・祝日の列が薄い赤になり、日付の文字も赤い
- [ ] 土曜の列が薄い水色になり、日付の文字も青い
- [ ] 土曜が祝日の日は、水色ではなく赤になる
- [ ] 今日が土日祝と重なる日は、黄色（今日）が優先される
- [ ] 左の「担当者 / 案件」欄の幅が、案件名を書くのに足りている

### 祝日の確認（CLAUDE.md 5.3 / `js/holidays.js`）

2026年で赤くなるはずの日:

- [ ] 8/11（山の日）
- [ ] 9/21・9/22・9/23（敬老の日／国民の休日／秋分の日）… 土日と合わせて5連休
- [ ] 10/12（スポーツの日）
- [ ] 11/3（文化の日）／11/23（勤労感謝の日）

2027年は**暫定値**です。春分の日・秋分の日は前年2月の官報で正式決定されるため、
2026年2月の発表を確認して `js/holidays.js` の ★印の行を修正してください。

- [ ] 2027/3/21（春分の日・★暫定）／3/22（振替休日・★暫定）
- [ ] 2027/9/23（秋分の日・★暫定）

### まだ無い部分（想定どおり）

- [ ] ［設定］［非表示を表示］［書き出し］［読み込み］は画面に無い（V1-c / V1-d で追加）
- [ ] 左下に「案件行はV1-cで実装します。」と出ている（V1-c で案件行に置き換わる）

---

## V1-b: データ層

**画面の見た目は変わりません。** データを作る・直す・保存する仕組みだけを作った段階です。
目視で確かめられるものが無いため、代わりにテストで検証しています。

es さんの確認は次の1つだけです。

- [ ] ターミナルで下のコマンドを実行し、「すべて通りました。」と出る

```bash
node tests/store-test.js
```

### テストで確認している内容（157項目）

| 分類 | 主な確認内容 |
|---|---|
| 日付変換 | CLAUDE.md 4.1 の例（2020-01-01午前=0 など）、1200日ぶんの往復、夏時間のある地域でもずれないこと |
| 部署 | 追加・改名・削除、担当者がいる部署は削除できないこと |
| 担当者 | 追加・改名・絵文字・コメント・部署異動・削除、唯一の担当になっている案件があれば削除できないこと |
| 案件 | 新規追加（タイトル空・バー1本・ラフ/未着手/今日1日）、複数担当、非表示の切替 |
| 担当0名の拒否 | 担当者を0名にする操作が拒否され、拒否後もデータが元のまま残ること |
| バー | 工程・状態・期間の変更、入稿と納品が常に1日幅になること、一覧に無い工程・状態の拒否 |
| migrate | 省略された項目の補完、ずれたバーの丸め、壊れたJSON・矛盾したデータの拒否 |
| 入出力 | 書き出し→読み込みで内容が戻ること、ファイル名 `sgantt-backup-YYYYMMDD-HHMM.json` |
| 読み込み失敗 | 壊れたファイルを読ませても、いま入っているデータが消えないこと |

### 補足

- テスト自体が正しく働くかを確認するため、`store.js` の規則をわざと5か所壊して実行し、
  すべてテストが失敗することを確認しています（確認後、元に戻してあります）。
- V1-b の時点では画面から呼び出していないため、**入力したデータは画面には出ません**。
  実際に触れるようになるのは V1-c（表示）・V1-d（編集）です。
