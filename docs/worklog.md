# 作業メモ（worklog）

セッションをまたいで引き継ぐための作業状態のメモです（CLAUDE.md 9-10 / 9-11）。

**仕様・決定事項はここに書きません。**それらは CLAUDE.md にのみ書きます（二重管理の禁止）。
このファイルに書くのは次の3点だけです。

- ロードマップ上の到達点
- 未解決の課題・バグと再現手順
- 次にやること

最終更新: 2026-08-06

---

## 到達点

ロードマップ **V2-c まで完了**。本番（GitHub Pages）で稼働中、`dataVersion` 4。
残りは **V2-d**（完了条件に「1週間以上の実運用の気づきの反映」を含む）と **V3**（共有）。

---

## 未解決の課題・バグ

バグとして把握しているものは**現時点なし**。以下は es の実運用での判断待ちの項目です。

### 1. 2027年の祝日が暫定値（要・官報確認）

- 内容: 春分の日・秋分の日は前年2月の官報で正式決定されるため、`js/holidays.js` の2027年分は天文計算にもとづく暫定値。
- 該当: `js/holidays.js` の `★暫定` コメント3行（2027-03-21 春分の日 / 2027-03-22 振替休日 / 2027-09-23 秋分の日）。
- 対応時期: 2026年2月の内閣府発表・官報を確認してから修正。

### 2. 掴みしろの縮退規則が現状の CSS では発動しない

- 内容: CLAUDE.md 5.10 の「バー幅24px未満で掴みしろ1/4、12px未満で掴みしろなし」は実装済みだが、`--col-width: 42px` が列幅の下限のため、バーの描画幅は常に42px以上になり条件に入らない。
- 再現手順: 期間を120日にしても列幅は42pxのまま。`css/style.css` の `--col-width` を一時的に10pxにすると縮退が働くことは確認済み。
- 位置づけ: 規則自体は正しく動作する（検証済み）。将来 `--col-width` を詰める場合の備え。バグではない。

### 3. 実データを見て判断したい見た目（es 確認待ち）

いずれも CSS カスタムプロパティ1か所で調整できます。

- TW `--mark-tw: #7cb87c` / 有休 `--mark-yukyu: #e08a8a` の文字色。白背景に対するコントラストが低め（約2.3:1・2.6:1）。
- 薄くした青 `--bar-wip-bg: #6b9bdd` とグレー `--bar-ok-bg: #9aa0a6` の上の白文字。コントラストは約2.9:1・2.6:1。
- TW・有休の日ハイライト `--day-tw` / `--day-yukyu` が土日祝の列色と見分けられるか。
- 伸縮の掴みしろ 6px（`js/render.js` の `HANDLE_PX`）が操作しやすいか。
- 行ラベル列の幅 `--label-width: 330px` が実際の案件名に足りるか。

### 4. 仕様どおりだが、使ってみると紛らわしい可能性のある挙動

- **今日に TW・有休を置くとハイライトが出ない**。CLAUDE.md 5.12 の優先順位（今日＞有休＞TW）どおり。
  - 再現手順: 今日の日付に TW のバーを置く → 列は今日の黄色のままで、薄緑は出ない。
- **文字ラベル工程は文字の部分だけがクリック対象**。CLAUDE.md 5.11 の「ラベルの矩形をクリック領域とする」に沿った実装のため、1日幅の枠内でも文字が無い箇所をクリックすると案件ポップアップ（5.6）が開く。
  - 再現手順: 入稿のラベルの、文字から少し離れた枠内をクリックする。
- **案件数の表示は括弧付き**（`（14）`）だが、5.13 の直接編集の入力欄には括弧なしの `14` が入り、保存値も `14`。

---

## 次にやること

**V2-d**（並び替えUI ▲▼・バー重なりの段積み表示の要否判断・部署折りたたみの要否判断・色や余白やスマホ閲覧の調整）。着手前に es の1週間以上の実運用の気づきを集める。
