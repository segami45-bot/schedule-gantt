# 作業メモ（worklog）

セッションをまたいで引き継ぐための作業状態のメモです（CLAUDE.md 9-10 / 9-11）。

**仕様・決定事項はここに書きません。**それらは CLAUDE.md にのみ書きます（二重管理の禁止）。
このファイルに書くのは次の3点だけです。

- ロードマップ上の到達点
- 未解決の課題・バグと再現手順
- 次にやること

最終更新: 2026-08-17

---

## 到達点

ロードマップ **V2-d の第1弾（実運用の気づき10点の反映）と、その追補 v2.14・v2.15 まで完了**。
`dataVersion` は 5。
V2-d の残りは **並び替えUI（▲▼）・バー重なりの段積み表示の要否判断・部署折りたたみの要否判断・
スマホ閲覧の調整**。その先は **V3**（共有）。

- v2.13: 仕様更新と実装3コミット（A: 磨き込み / B: 制作メモ・新規バーの強調 /
  C: 空白セルのクリック・MT のセル背景）。
- v2.14: 文字ラベル工程のクリック領域を1日セル全体に拡大・MT のツールチップ。
- v2.15: 空白セルのクリックを新規作成モード（下書き）に変更・MT の色を濃い黄に。
- `node tests/store-test.js` は 323 件パス。

---

## 未解決の課題・バグ

バグとして把握しているものは**現時点なし**。以下は es の実運用での判断待ちの項目です。

### 1. 2027年の祝日が暫定値（要・官報確認）

- 内容: 春分の日・秋分の日は前年2月の官報で正式決定されるため、`js/holidays.js` の2027年分は天文計算にもとづく暫定値。
- 該当: `js/holidays.js` の `★暫定` コメント3行（2027-03-21 春分の日 / 2027-03-22 振替休日 / 2027-09-23 秋分の日）。
- 対応時期: 2026年2月の内閣府発表・官報を確認してから修正。
- ［7営業日］の終了日はこの一覧を見て決めるため、祝日を直すと表示幅も変わります。

### 2.（解決済み）文字ラベル工程の枠内をクリックすると新規バーができる

- 「空白セル」の定義を明確化し、クリック領域を1日セル全体に広げて **v2.14 で解消**。

### 3. 掴みしろの縮退規則が現状の CSS では発動しない

- 内容: CLAUDE.md 5.10 の「バー幅24px未満で掴みしろ1/4、12px未満で掴みしろなし」は実装済みだが、`--col-width: 42px` が列幅の下限のため、バーの描画幅は常に42px以上になり条件に入らない。
- 再現手順: 期間を120日にしても列幅は42pxのまま。`css/style.css` の `--col-width` を一時的に10pxにすると縮退が働くことは確認済み。
- 位置づけ: 規則自体は正しく動作する（検証済み）。将来 `--col-width` を詰める場合の備え。バグではない。

### 4. 実データを見て判断した見た目（2026-08-17 es 確認済み）

いずれも CSS カスタムプロパティ1か所で調整できます。

- MT のセル背景: 薄すぎて見分けられなかったため、**v2.15 で濃い黄 `--day-mt: #ffd966` に変更し解消**。
- TW `--mark-tw: #7cb87c` / 有休 `--mark-yukyu: #e08a8a` の文字色。白背景に対するコントラストが低め（約2.3:1・2.6:1）。2026-08-17 es 確認済み・現状維持で確定。
- 薄くした青 `--bar-wip-bg: #6b9bdd` とグレー `--bar-ok-bg: #9aa0a6` の上の白文字。コントラストは約2.9:1・2.6:1。2026-08-17 es 確認済み・現状維持で確定。
- TW の日ハイライト `--day-tw` が土曜の水色と見分けられるか（有休は日祝と同色にそろえた）。2026-08-17 es 確認済み・現状維持で確定。
- 伸縮の掴みしろ 6px（`js/render.js` の `HANDLE_PX`）が操作しやすいか。2026-08-17 es 確認済み・現状維持で確定。
- 行ラベル列の幅 `--label-width: 330px` が実際の案件名に足りるか。2026-08-17 es 確認済み・現状維持で確定。
- 新規バーの強調表示の色（`css/style.css` の `.barrow.is-new`）が分かりやすいか。2026-08-17 es 確認済み・現状維持で確定。

### 5. 仕様どおりだが、使ってみると紛らわしい可能性のある挙動

いずれも 2026-08-17 es 確認済み・現状維持で確定。

- **［7営業日］は金曜に押すと幅が11日になる**（土日を2回はさむため）。CLAUDE.md 5.2 の「通常9日」は月〜木に押した場合。2026-08-17 es 確認済み・現状維持で確定。
- **担当者ヘッダのプレースホルダーはホバー中しか出ない**ため、タッチ操作では空項目に気づけない（CLAUDE.md 5.13 のとおり、その場合は設定モーダルで入力）。2026-08-17 es 確認済み・現状維持で確定。
- **案件数の表示は括弧付き**（`（14）`）だが、5.13 の直接編集の入力欄には括弧なしの `14` が入り、保存値も `14`。2026-08-17 es 確認済み・現状維持で確定。
- **［バーを追加］で増えた行は開始日順に並ぶ**ため、必ずしも一覧の最後には来ない（強調表示で見つける）。2026-08-17 es 確認済み・現状維持で確定。
- **色付きバーのすぐ上・すぐ下（各3px）をクリックしても何も起きない**。その日はもうバーがあるため空白セルではなく（CLAUDE.md 5.5）、かつバーの描画部分でもないため。2026-08-17 es 確認済み・現状維持で確定。

---

## 次にやること

**V2-d の残り**（並び替えUI ▲▼・バー重なりの段積み表示の要否判断・部署折りたたみの要否判断・
色や余白やスマホ閲覧の調整）。上の「4.」「5.」は 2026-08-17 に es 確認済みで、
MT の色以外は現状維持で確定した。
